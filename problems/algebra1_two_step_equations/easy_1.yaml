topic: algebra1/two_step_equations
difficulty: easy
calculator: none
variables:
  a: nonzero(-8, 8)
  b: integer(-20, 20)
  c: integer(-10, 10)
  x_val: integer(-10, 10)
  rhs: a * x_val + b
  answer: x_val
constraints:
  - abs(rhs) <= 50
  - gcd(abs(a), abs(rhs - b)) == abs(a)
question: "Solve for x: {equation(a*x + b, rhs)}"
answer: answer
solution:
  - "Start with {equation(a*x + b, rhs)}"
  - "Subtract {b} from both sides: {equation(a*x, rhs - b)}"
  - "Divide both sides by {a}: x = {answer}"