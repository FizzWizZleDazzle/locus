topic: algebra1/linear_inequalities
difficulty: easy
calculator: none

variables:
  a: nonzero(-6, 6)
  b: integer(-10, 10)
  c: integer(-10, 10)
  answer: solve(a*x + b - c, x)

constraints:
  - a != 1
  - a != -1
  - b != c

question: "Solve for x: {equation(a*x + b, c)}"

answer: answer

solution:
  - "Start with {a}x + {b} = {c}"
  - "Subtract {b} from both sides: {a}x = {c} - {b}"
  - "Divide by {a}: x = {answer}"
